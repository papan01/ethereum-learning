# @wagmi/connectors → WalletConnect / AppKit use Preact as a peer; pnpm keeps it nested so
# Next.js often cannot resolve `preact`. Hoist it to the store root layout without adding it to apps/web.
public-hoist-pattern[]=*preact*
